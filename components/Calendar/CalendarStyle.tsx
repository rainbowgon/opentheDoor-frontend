import styled from 'styled-components/native';
import { theme } from "../../styles/colors";

export const TimeListView = styled.View`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;

  overflow: hidden;
  height: fit-content;
  width: auto;
  margin: 10px 30px;

  border-radius: 5px;
  `;